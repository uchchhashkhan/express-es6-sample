version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm ci
  build:
    commands:
      - npm test --if-present

artifacts:
  files:
    - '**/*'
  discard-paths: no
